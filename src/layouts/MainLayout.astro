---
import Container from "@components/Container";
import Footer from "@components/Footer";
import Header from "@components/Header";

// export interface Props {
// 	title?: string;
// }

const { title } = Astro.props ;
---

<!DOCTYPE html>
<html class="dark" lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/x-icon" href="/favicon.ico" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body class="dark:bg-primary bg-soft-white dark:text-soft-white">
    <div class="min-h-screen grid grid-rows-[auto_1fr_auto] grid-cols-[100%] overflow-hidden">
      <Header client:load/>
      <main>
        <Container className="py-5 md:pt-10 md:pb-16">
          <slot />
        </Container>
      </main>
      <Footer/>
    </div>
  </body>
</html>

<!-- <script is:inline>
  const getTheme = (() => {
    if (
      typeof window.localStorage !== 'undefined' &&
      localStorage.getItem('theme')
    ) {
      return localStorage.getItem('theme');
    }
    if (window.matchMedia('(prefers-color-scheme: dark)').matches) {
      return 'dark';
    }
    return 'light';
  })();

  if (getTheme === 'light') {
    document.documentElement.classList.remove('dark');
  } else {
    document.documentElement.classList.add('dark');
  }

  window.localStorage.setItem('theme', getTheme);
</script> -->

